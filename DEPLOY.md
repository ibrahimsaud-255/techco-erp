# النشر

المشروع static بالكامل (HTML + JS + Supabase) — لا يحتاج خطوة build.

## 1) إنشاء مستودع GitHub جديد للشركة
```bash
git remote add origin <رابط المستودع الجديد>
git push -u origin main
```

## 2) خيارات الاستضافة (أي واحدة)
- **GitHub Pages:** فعّله من إعدادات المستودع → Pages → فرع `main` / المجلد الجذر.
- **Netlify / Vercel / Cloudflare Pages:** اربط المستودع، بدون أمر build، ومجلد النشر = الجذر.

## 3) الدومين الجديد
- ضع دومين الشركة في ملف `CNAME` (سطر واحد) عند تسجيله، أو اضبطه من لوحة الاستضافة.
- إن استخدمت GitHub Pages: أضف سجلّات A للنطاق إلى عناوين GitHub Pages
  (`185.199.108.153`, `185.199.109.153`, `185.199.110.153`, `185.199.111.153`)
  و CNAME لـ `www` يشير إلى `<حسابك>.github.io`.

## 4) قاعدة البيانات
- تأكد أن `SUPA_URL` و `SUPA_KEY` في `app/index.html` يشيران لمشروع Supabase الجديد قبل النشر (راجع README).

## التحديث السريع لاحقاً
```bash
./deploy.sh "وصف التحديث"
```
