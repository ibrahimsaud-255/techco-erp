# طريقة العمل المشتركة (إبراهيم + تركي)

المستودع: <https://github.com/ibrahimsaud-255/techco-erp> (عام)
الفرع الرئيسي: `main` — كل دفعة (push) عليه تُنشر تلقائياً على **minwal.tech**.

---

## مرة واحدة: نسخ المشروع على جهازك

**الأسهل لغير المبرمج — GitHub Desktop:**
1. ثبّت GitHub Desktop وسجّل دخولك بحساب GitHub.
2. File → Clone repository → اختر `ibrahimsaud-255/techco-erp` → Clone.

**أو عبر التيرمنال:**
```bash
git clone https://github.com/ibrahimsaud-255/techco-erp.git
cd techco-erp
```

> إبراهيم: مجلدك الحالي `التقنية_نظام` هو نفسه المستودع — لا تحتاج clone، فقط ارفع (أمر الرفع جاهز لك).

---

## دورة العمل اليومية (بسيطة — فريق صغير)

قبل ما تبدأ أي تعديل، **اسحب آخر نسخة** عشان ما تتعارض مع شغل شريكك:
```bash
git pull
```

عدّل الملفات (الواجهة في `app/index.html`، لوحة التحكم في `admin/`، قاعدة البيانات في `supabase/`).

بعد ما تخلّص:
```bash
git add -A
git commit -m "وصف مختصر للتعديل"
git push
```

خلال 1–2 دقيقة يتحدّث الموقع على minwal.tech تلقائياً.

**في GitHub Desktop:** نفس الفكرة بالأزرار — Pull → عدّل → اكتب وصفاً → Commit → Push.

---

## نصائح لتفادي التعارض

- **اسحب (`git pull`) قبل ما تبدأ، وادفع (`git push`) بسرعة بعد ما تخلّص.**
- نسّقوا: لا تشتغلون على نفس الملف بنفس الوقت قدر الإمكان.
- لو ظهر تعارض (conflict) عند الدمج، GitHub Desktop يبيّن لك الأسطر المتعارضة لتختار الصح.

### للتعديلات الكبيرة (اختياري — أأمن)
بدل التعديل المباشر على `main`، اشتغل على فرع منفصل ثم ادمجه عبر Pull Request:
```bash
git checkout -b feature/اسم-الميزة
# ... عدّل وادفع ...
git push -u origin feature/اسم-الميزة
```
ثم من GitHub: افتح Pull Request → راجعوه سوياً → Merge.

---

## تقسيم الأدوار المقترح
- **تركي:** قاعدة البيانات (`supabase/`)، الربط، الدوال، التطبيقات الجديدة.
- **إبراهيم:** الواجهة والهوية والتجربة (`app/`, `admin/`)، المحتوى.
- القرارات المعمارية الكبيرة في `docs/الأساس-المعماري.md`.

---

## مهم
- لا تضعوا مفاتيح سرية (Service Role / كلمات مرور) في الكود — المستودع عام. المفتاح الموجود (`SUPA_KEY`) عام (publishable) وآمن.
- أي تعديل على قاعدة البيانات: احفظوه كملف SQL في `supabase/` (آمن لإعادة التشغيل) ليبقى موثّقاً للطرفين.
